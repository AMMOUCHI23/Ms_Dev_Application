import React from 'react'
import ReactDOM from 'react-dom/client'
import Categories from './jsx/Categories';

ReactDOM.createRoot(document.getElementById('react-app')).render(
    <React.StrictMode>
        <Categories/>
    </React.StrictMode>,
)