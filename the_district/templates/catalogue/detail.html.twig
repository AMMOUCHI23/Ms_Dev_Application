{% extends 'base.html.twig' %}

{% block title %}Plats{% endblock %}

{% block body %}

 <div class="container  mt-5">
  <h1 class="text-center text-primary my-4"> Détail du Plat </h1>
        <div class="row justify-content-center mt-4 ">
 
    

             <div class="col-md-4 mx-3">
                    <img src="{{ asset('assets/img/category/' ~ plat.image) }}" class="card-img-top mt-4" alt="...">
                    </div>
                    <div class="col-md-5 mx-3">';
                    <h2 class="text">{{ plat.libelle }}</h2>
                    <h4 class="py-3">{{ plat.prix }} €</h4>
                    <h5 class="text-primary mt-3">Ingrédients :</h5>
                    <p>{{ plat.description }}</p>
                    <a href="{{ path("ajoutPanier",{id: plat.id})}}"><input type="submit" class="btn btn-primary  my-2" value="Ajouter au panier" name="addpanier"> <br></a>
                    <a href="../../index.php" class="btn btn-lg btn-primary my-5 ">Continuer les achats </a>
                    <a href="addPanier.php" class="btn btn-lg btn-success  ms-md-5 ">Passer la commande </a>
                   </div>
                    
                     
                  
   
       
    </div>
    </div>
{% endblock %}